apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tasks-persistent-volume-claim
spec:
  volumeName: tasks-persistent-volume
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard