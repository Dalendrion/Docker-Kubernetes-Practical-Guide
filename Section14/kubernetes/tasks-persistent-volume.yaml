apiVersion: v1
kind: PersistentVolume
metadata:
  name: tasks-persistent-volume
spec:
  hostPath:
    path: /data
    type: DirectoryOrCreate
  capacity:
    storage: 1Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  storageClassName: standard